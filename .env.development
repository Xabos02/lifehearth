# Дев-сервер и браузерные тесты НЕ должны попадать в боевой сервер: см.
# src/lib/workerUrl.ts. Здесь стоит адрес локального `wrangler dev` — если он
# не запущен, запросы просто никуда не уходят, и это правильное поведение.
VITE_WORKER_URL=http://127.0.0.1:8787
