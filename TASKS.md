# Задачи от Влада — набор от 10.09.2026

**Это пульт.** Статус меняется тем же коммитом, что и сама правка, поэтому файл
на GitHub всегда показывает настоящее положение дел, а не намерение.

| # | Задача | Статус |
|---|--------|--------|
| 16 | Заглавная буква после автонумерации (13.7) | **сделано и проверено** |
| 18 | Пустое поле даты выглядело заполненным (срочное) | **сделано и проверено** |
| 21 | Обучение под срок: сроки, часы, план курса | **сделано и проверено** |
| 17 | Экран дёргается при долгом наборе (13.8) | сделано, не проверено |
| 15 | Раздел «Задачи» целиком — вбирает 1, 2, 4, 5, 7, 9 | **сделано и проверено** (на проде 1.25.0–1.26.0); 3 пункта отложены |
| 11 | Семейный чат перегружен | **сделано и проверено** (на проде 1.34.0–1.35.0): шапка со «шторой» (Abacus, мимо канвы), баннер «группы нет» над шторой |
| 12 | Иконки: таб-бар и весь набор | **сделано и проверено** — глифы на видных местах, lucide в глубине под единым весом (PROTOCOL §4) |
| 13 | Обучение (онбординг), пункты 1-6 | **сделано и проверено** (на проде 1.23.0): 2, 5, 6 — рисованные картинки и иконка крупно; 1 — развилкой, решать вместе с 10 |
| 10 | Упростить вход, синхронизацию, восстановление | канва готова, ждёт выбора Влада |
| 6 | Раздел «Фокус» — разбор и переделка | **сделано и проверено** (на проде 1.22.0); 12 находок закрыты, 5 отложены; беззвучный режим iPhone — проверить Владу |
| 3 | Файлы любых форматов во вложениях | **сделано и проверено** |
| 14 | Центр здоровья и спорта — новый раздел | **сделано и проверено** (на проде 1.24.0); 33 из 40 находок разбора закрыты, 7 отложены |
| 20 | Заметки: множественный выбор и порядок папок | **сделано и проверено** |
| 22 | «Фокус» по скринам Focus To-Do: мелодии, громкость, шумы, за 5 минут, весь экран | **сделано и проверено** (на проде 1.25.0) |
| 8 | Сквозной прогон всего приложения | **сделано и проверено** (на проде 1.27.0): 127 находок, 40 закрыто, остальное — на пульте по темам |
| 23 | Задачи с iPhone не появлялись на маке; обновления между устройствами — за секунды | **сделано и проверено** (на проде 1.28.0) |
| 24 | Кнопка «+» в «Задачах» не давала завести проект (побочная находка задачи 18) | **сделано и проверено** (на проде 1.31.0) |
| 25 | Спорт: составные тренировки, шаблоны, свои упражнения | сделано, не проверено — разбор 24.09 и 4 починки в 1.36.0, ждёт деплоя |
| 26 | Семья: задачи с цветом, приоритетом и напоминаниями; чат на весь экран | сделано, не проверено — разбор 24.09 и 14 починок в 1.36.0, ждёт деплоя; 3 развилки — РЕШАЕТ ВЛАД (задача 32) |
| 27 | «Главная» по пять разделов и обязательный экран приоритета | **сделано и проверено** (на проде 1.35.0: запись в «Что нового», стрелки разведены) |
| 28 | Удалить семейную группу целиком | **сделано и проверено** (на проде 1.35.0, воркер выкачен) |
| 29 | Окно создания задачи плавает вбок и пружинит | сделано, не проверено — на проде 1.35.0; ЧЕСТНАЯ ГРАНИЦА: палец на iPhone проверяет Влад |
| 30 | Повтор «каждый месяц» ставил 1-е число вместо дня срока | **сделано и проверено** (правка на проде с 1.33.0, тест напоминания и запись — 1.35.0) |
| 31 | Тап по уведомлению вёл на старый адрес /life-hub/ (с переезда 07.09) | сделано, не проверено — на проде 1.35.0; ЧЕСТНАЯ ГРАНИЦА: тап на iPhone проверяет Влад |
| 32 | Хвосты сквозного прогона (задача 8) и разбор работы 20.09 | сделано, не проверено — 1.36.0, ждёт деплоя; развилки — РЕШАЕТ ВЛАД ниже |

Статусы: `не начата` / `в работе` / `сделано, не проверено` / **`сделано и
проверено`**. «Готово» пишется только после проверки — как и в работе.

**Порядок и почему такой.** Сверху два ежедневных раздражителя: они мелкие, а
мешают каждый раз, когда Влад пишет текст. Дальше раздел «Задачи» одним заходом,
потому что шесть пунктов набора — про него, и делать их поштучно значит трогать
одни файлы дважды. Потом визуальные заходы (чат, иконки, обучение), потом
крупные переделки, потом новый раздел. Сквозной прогон последним: он проверяет
состояние на момент прогона, а всё выше это состояние меняет.

Порядок меняется одним словом Влада.

---

## 1. Порядок в списке напоминаний · **сделано и проверено**

> Надо начинать сначала с прошлого, за сколько, а потом показывать настоящее

Список идёт от момента задачи в прошлое (Вовремя → за 5 мин → … → за неделю).
Развернуть по ходу времени: сверху «за неделю», ниже по убыванию до «за 5 мин»,
в конце «Вовремя». «Выкл» остаётся отдельным первым пунктом.

## 2. Чёрная полоса над нижней панелью · **сделано и проверено**

> Вот это темное поле нужно убрать вообще, его небыло, не давно появилось

Экран «Задачи»: широкий тёмный прямоугольник между списком и таб-баром,
обрезает текст на полуслове. Появился недавно — найти, откуда, и убрать совсем.

Найден: коммит `4b033b8` от 22.08 — лента укорачивалась на 68px, чтобы «+» не
воровал нажатия. Убрано коммитом `a9879f9`; заняло три подхода, потому что
экран внутри flex-обёртки УЖИМАЛСЯ до её высоты (510px при содержимом 1513) и
любой отступ вставал на 480px выше конца контента. Решение — `shrink-0` на
экране. Тест `layout.spec.ts` переписан под то, что важно человеку.

## 3. Файлы любых форматов во вложениях задач · **сделано и проверено**

> Добавить возможность в задачи добавлять не только картинки но и файлы любых
> форматов и по максимуму обьем

Упирается в устройство хранения: вложение лежит внутри записи задачи, а запись
тяжелее ~1,6 МБ на сервер не уезжает (`RECORD_MAX_BYTES`, src/lib/sync.ts) —
отсюда строка «не уезжает записей» в настройках. Значит нужен перенос вложений
в отдельную таблицу кусками, как уже сделано для заметок. Реальный предел
назвать после разбора.

Сделано коммитом `b07e51e`: таблица `taskFiles` по образцу `noteFiles`, схема
v22, синхронизация, резервная копия, корзина. Предел 8 МиБ на файл — как у
заметок и чата, один на всё приложение. Раздел «Файлы» в форме задачи под
«Фото», карточка общая с заметками. 5 юнитов + 2 сквозных.

## 4. Направляющие вложенности справа · **сделано и проверено**

> Вот эти цветные полосы слева мне нравятся прикольно сделал). Но с правой
> стороны посмотри, как они выглядят, исправь. Я даже не знаю справа они нужны
> или нет, ты как думаешь?)

Разобрано с владельцем на трёх вариантах рядом. Итог: загибы слева остаются
(его выбор, закреплён тестом и комментарием), скругления справа сняты
(`rounded-l-2xl`). Что именно проявляло правые уголки на его экране — не
найдено (замеры дали 0px со всех сторон), но владелец подтвердил: исчезли.
Коммиты `314f6e9`, `a84f701`.

## 5. Прикрепление проекта к проекту вместе с поддеревом · **сделано и проверено**

> Нужно сделать возможность Проект и даже если у проекта есть подпроект
> прикрепить к другому проекту, и тогда проект который мы хотим прикрепить
> должен стать по логике под проектом проекта к которому мы хотим прикрепиться

Два решения за Владом, всплывут при выполнении: предельная глубина вложенности
(перенос легко делает 4-5 уровень, на телефоне это перестаёт помещаться) и
запрет прикреплять проект к собственному потомку — иначе дерево замыкается в
кольцо.

## 6. Раздел «Фокус» — разбор и переделка · **сделано и проверено**

> Необходимо проверить и оптимизировать раздел фокус, нету выбора звука
> уведомления и т.п., нету выбора количества кругов(раз) и над остальным тоже
> подумай и улучши все перепроверь, протестируй

Самая крупная из десяти. «Подумай над остальным» = сначала разбор раздела
целиком, потом переделка.

**Разбор 11–12.09.2026** — четыре агента по движку, экрану, тестам и «как у
лучших» (Apple Clock, Focus To-Do, Forest); 31 находка. Канва с экраном —
«Фокус — сигнал и круги», артборды в `design/focus/`. Выбран вариант А:
точки кругов под меткой фазы.

**Сделано (релиз 1.22.0):**

- Сигнал в конце круга — свой ряд: мягкий, колокольчик, двойной, гонг, без
  звука. Тап по варианту его проигрывает. «Звук фокуса» переименован в
  «Фоновый шум» — это было главное недоразумение: ряд с белым/розовым шумом
  читался как выбор сигнала, а сигнал был один и зашит.
- Кругов до длинного перерыва — степпер 1–8 вместо застывшей фразы «после
  каждых 4». Под меткой фазы — точки цикла: закрашено сделано, контур идёт.
- Круг, дошедший до конца в фоне, засчитывается молча по реальному времени:
  раньше по возвращении играл сигнал «Фокус завершён» и перерыв начинался с
  этой секунды, даже спустя час.
- Уведомление о конце круга — своё на каждом устройстве (было одно на всю
  семью: чужой старт перебивал, чужая пауза снимала).
- «Завершить круг» через 10 секунд после старта больше не даёт +1 круг и +25
  минут; в простое кнопка неактивна (раньше запускала перерыв из ничего).
- Тап по цифрам в центре кольца не меняет длительность (было: 25 → 55 одним
  касанием); на паузе кольцо показывает прогресс, а не превращается в
  регулятор, стирающий остаток.
- Выбранный в простое фоновый шум звучит 2,5 секунды на пробу — иначе тап по
  чипу давал тишину.
- Внизу экрана баннер, если уведомления выключены, с включением на месте.
- Счётчики дня сбрасываются и при переходе через полночь в открытом
  приложении; цикл кругов — с новым днём.
- Крестик «Убрать таймер» с полоски над меню снят: он оставлял пустую полосу
  под кнопкой «+» до конца фазы.
- «помодоро сегодня» → «кругов сегодня»; в полоске — «Длинный перерыв».

Проверено: 8 юнитов докрутки/цикла, 8 браузерных тестов «Фокуса» (три
проверены мутацией: убираешь докрутку, границу кольца, паузу — тесты краснеют).

**Честные границы (проверить может только Влад на iPhone):**

- В свёрнутом PWA своего звука не будет никогда — придёт системное
  уведомление со стандартным звуком. Так у всех веб-приложений на iOS.
- Сигнал и шум идут через Web Audio; iOS глушит его беззвучным переключателем.
  Поставлен `navigator.audioSession.type` (Safari 17+), который должен это
  снимать, — на устройстве не проверено. Просьба: включить беззвучный режим,
  запустить круг на 1 минуту, дождаться сигнала.
- Вибрации на iPhone нет ни в одном браузере, поэтому пункта «только
  вибрация» не сделано.

**Отложено (не в этом заходе):**

- Настройки и шаблоны «Фокуса» живут в памяти устройства, вне копии и синка:
  второй телефон или переустановка их не увидят. Перенос в базу — отдельная
  работа с миграцией.
- История фокуса по дням (сейчас только «сегодня»).
- Авто-старт следующего круга после перерыва (переключатель, как у Forest).
- «Сбросить» без подтверждения рядом со «Старт» — снимает и задачу, и цикл.
- Список задач в выборе для фокуса — без порядка и поиска.

## 7. Кнопка «+ задача» у заголовка проекта · **сделано и проверено**

> И необходимо добавить возможность добавление задачи и чтобы эта возможность
> стояла возле проекта возле редактирования. Так как когда много задач мне
> приходиться листать в самый низ, чтобы добавить задачу в этот проект, можно
> просто сделать + или что то типо этого.

Рядом с карандашом. Нижнюю «+ Задача» оставляю, если Влад не скажет убрать.

Сделано коммитом `d965bd8`: «+» перед карандашом у проектов и подпроектов,
форма открывается с выбранным проектом. Зазор gap-5 — тест зон касания поймал
перекрытие в 2px при gap-4.

## 8. Сквозной прогон — постановка (отчёт и статус — ниже, второй раздел 8)

> Прощелкай все разделы, все пункты и проверь на исправность

Ставится в конец намеренно: задачи 1-7 меняют приложение, и прогон, сделанный
раньше, устареет к вечеру. Найденные ляпы чиню сразу, а не завожу списком.

## 9. Счётчик задач в цвет проекта · **сделано и проверено**

> И я вот думаю может кол-во задач которые отображаются в проекте номером
> сделать в цвет проекта?

Мой ответ: в лоб — нет, приглушённо — да. Цвет уже занят иконкой папки и левой
направляющей; если красить ещё и число, цвет отвечает за две разные вещи.
Плюс мелкое тёмно-синее или красное число на тёмном фоне читается хуже серого.
Сделать оба варианта, замерить контраст, показать рядом на настоящих проектах.

Показано на канве три варианта в двух темах на настоящих проектах Влада; он
выбрал цифру в цвете проекта. Сделано коммитом `ffabaf0` — в тёмной теме цвет
проекта, в светлой прежний серый (цветная там проваливает AA для всех 27
цветов — замер). Оба поведения закреплены тестом.

## 10. Упростить вход, синхронизацию и восстановление · канва готова, ждёт выбора Влада

> Надо сделать более минималистичным и упростить до максимума регистрацию,
> синхронизацию и восстановление аккаунта в приложении. Слишком сложно для
> обычного пользователя особенно для женщин. Нужно чтобы было просто, понятно,
> минималистично и быстро

Напряжение названо заранее: выпуск 1.15.0 сделал путь понятнее, но не короче —
добавился шаг проверки ключа. Он не для красоты: без него человек уходит с
зелёной галочкой и нерабочим файлом. Резать надо всё остальное — экраны, слова,
выборы, — а не эту проверку. Где именно резать, показать разбором до правок.

Посчитано 11.09: три экрана синхронизации — ~460 слов. Канва «как сейчас (246
слов) vs минимум (22 слова)»: https://claude.ai/code/artifact/bbb97191-4945-40d4-b3c7-a212ac523344
Минимум: «Записи на всех ваших устройствах · Обновлено сегодня в 12:40»,
строка ключа с состоянием, кнопка «Подключить другое устройство», всё прочее
под «Подробнее». Ничего не удаляется — прячется. РЕШАЕТ ВЛАД: делать ли так.

## 11. Семейный чат перегружен · **сделано и проверено**

> Нужно сделать больше минимализма, слишком нахоамлено, загружен экран

Над первым сообщением четыре ряда управления подряд: шапка с названием и
кнопками, лента групп чипами, вкладки «Чат / Задачи / Участники», строка «был в
сети». Вместе съедают треть экрана — переписка начинается за перегибом.

Канва с тремя компоновками: https://claude.ai/code/artifact/35664bdd-e8ac-48aa-8bb0-dd5742b81f5d
В — как сейчас; А — группы уходят в шапку (название = переключатель), лента
чипов исчезает; Б — вкладки уходят в иконки шапки, чипы остаются. Оба переносят
«был в сети» в подпись шапки и оставляют два ряда. РЕШАЕТ ВЛАД: что важнее
держать под пальцем — переключение групп или переход к задачам/участникам.

Сделано 20.09 в Abacus (1.34.0, `a56e494`), мимо канвы: ни А, ни Б, а
третий вариант. Шапка в одну строку, группы, вкладки и уведомления — в
«шторе» за шевроном справа, «был в сети» — подписью шапки; чат открывается на
весь экран без таб-бара (1.33.0). Влад принял результат 24.09.

Дыра, найденная сверкой 24.09: баннеры «Вас исключили» и «Группу удалил
создатель» жили внутри «шторы», а она по умолчанию свёрнута — человек видел
живой на вид чат, в который ничего не приходит. Вынесены над «шторой»
(1.35.0). `e2e/family-gone.spec.ts` проверяет, что баннер видно глазом, а не
только в дереве: `toBeVisible()` такую прозрачную обрезанную надпись
пропускал, новая проверка на старом коде падает.

Долг: артборда нового вида шапки в `design/` нет.

## 12. Иконки: таб-бар и весь набор · **сделано и проверено**

> Нужно проработать эти иконки, их дизайн, цвет и т.п. Так как они выглядят
> очень скучно и старо. Сделай премиальные иконки, красивые, стильные,
> современные. И все остальные иконки в приложении тоже просматри. У тебя есть
> все для этого инструменты, а также design, используй все необходимые скилы.

Сделано, и давно — пульт этого не видел. Работа шла коммитами с 27.07 по
21.08 (`293eb06` собственные глифы вместо lucide там, где их видно;
`86a719c` панель редактора; `a782f29` премиальная отделка и единый набор
вместо смеси; `5c8cf69`, `d868d39` заливки и ещё сорок мест), а запись
«не начата» в реестре пережила все они. Тот же класс ошибки учёта, что и в
задаче 19: статус не сверялся с кодом.

Как решено. Перерисовать все ~150 знаков одной рукой нельзя и не нужно:
редкие знаки в глубине настроек смесь наборов не выдаёт. Вместо этого —
свои глифы на всём, что человек видит постоянно (таб-бар, плитки разделов,
панель форматирования, частые действия), а lucide остаётся только в глубине,
но под единым весом: `LucideProvider` с `absoluteStrokeWidth` в `App.tsx`,
штрих 1.7/2/2.4 пикселями готового рисунка, ступени размера в `ui/icons.ts`.
Правила набора и граница записаны в PROTOCOL.md §4 (писался уже по готовому
коду).

Проверено 20.09, отдельно, потому что пульт лгал: инвентарь — своих глифов 54,
lucide 100 уникальных в 55 файлах (глубина настроек); новый раздел «Спорт»
(`c524b6c`) прямых импортов lucide не тянет вовсе — только общий реестр.
Визуально в браузере: таб-бар, экран задач и список разделов — один штрих,
скруглённые концы, залитые узлы активной вкладки, смесь наборов не видна.
Метрический тест `icons.spec.ts` — 5/5: штрих в пикселях на любом размере,
размеры только со ступеней, шапочные кнопки одной ширины.

## 13. Обучение (онбординг) — восемь пунктов · **сделано и проверено** (пункт 1 — вместе с 10)

> 1. Перед обучением, сначала человек должен войти в свой аккаунт, потом уже
>    должно идти обучение
> 2. на первом слайде обучения должен быть наша картинка приложения большая
> 3. Нету возможности вернуться на предыдущий слайд
> 4. текст не равномерно по распределен, нанесен абы как все в перемешку все
>    сливается, пользователю трудно понять что происходит и что написано.
>    Проработай текст во всех смыслах, у тебя для этого есть все необходимые скилы.
> 5. Про раздел семья нужен отдельный слайд, и нужно поставить также большую
>    картинку передающую тепло и семью и связь с ними, скилы у тебя тоже есть
> 6. Картинки тоже проработай на всех слайдах, замени, создай, в общем мне нужен
>    результат, все должно быть премиально, минималистично и дорого
> 7. видишь сейчас все по пунктам перечислил и когда я хочу перейти на следующую
>    нумерацию, я нажимаю на интер, и нумерация проставляется автоматически, но
>    когда я начинаю писать текст первое слово не начинается с большой буквы,
>    хотя должно начинаться с большой автоматически, исправь.
> 8. вот щас пока все это писал заметил что у меня дергается экран приложения во
>    время письма продолжительного. Нужно тоже по фиксить

Пункт 1 пересекается с задачей 10: в 1.15.0 на последний слайд знакомства уже
добавлена развилка «У меня уже были данные — восстановить». Влад предлагает
сильнее — вход до обучения. Для переезжающего верно, для нового человека вход
первым экраном это стена до того, как он понял, зачем приложение. Вероятный
ответ — развилка на первом экране «впервые» / «уже есть аккаунт». Решать вместе
с десятой.

Пункты 7 и 8 к обучению не относятся — это поведение ввода текста (заглавная
после автонумерации; дёрганье экрана при долгом наборе). Записаны здесь по
нумерации Влада, делаются отдельно и, скорее всего, раньше: мелкие, бьют каждый
день.

Сделано 11.09 (коммит `fa1a46c`): п.3 — «Назад» со второго слайда и свайп в обе
стороны; п.1 — развилкой, «У меня уже были данные — восстановить» видно на
первом экране и на последнем. Коммит `a4b3c91`: п.4 — тексты переписаны,
11-16 слов на слайд вместо 35-45, одна мысль на экран; п.5 — у семьи свой
слайд.

Сделано 12.09, пункты 2, 5 (картинка), 6 — канва «Обучение — картинки
слайдов», артборды в `design/onboarding/`. Первый слайд — иконка приложения
160px с тёплой тенью от углей. Остальные семь — рисованные иллюстрации в
одном стиле: тонкий серебристый штрих и свечение очага снизу, как на иконке
(«L» над углями). Семья — трое у общего очага: две фигуры и маленькая между,
огонь один на всех. Вектор, красится под тему (светлая проверена на канве),
весит килобайты.

Почему рисунок, а не генерация: генератор картинок сегодня не поднимается,
но главное — фотореалистичная «семья у камина» на тёмном минималистичном
экране выглядит чужой, а восемь таких — восемь чужих. Если Влад захочет
живые иллюстрации — композиции и стиль заданы, по ним можно рисовать.
Проверено браузерным тестом: иконка ≥150px, на семи слайдах семь разных
рисунков; контраст и зоны касания зелёные.

## 14. Центр здоровья и спорта — новый раздел · **сделано и проверено**

> Нужно создать раздел, что то вроде центра управления здоровьем, спорт. Иам
> должен быть коризантальный мини календарь и возможностью менять масштаб,
> месячный, недельный. Чтобы такие пользователи как я с гибким графиком работы,
> после каждой тренировки отмечали свою тренировку и в календаре спортивном я
> видел когда последний раз я тренировался, так как я стараюсь тренироваться
> через день три раза в неделю. Также возможность подтягивать из других
> спортивных приложений данные или же самому вводить данные о тренировке чтобы
> потом видеть свой прогресс по всем пунктам. По вопросам здоровья буду ждать от
> тебя предложений, все эти мои идеи нужно разведать как у лучших компаний и
> приложений в этих областях чтобы мы равнялись на лучших и брали фундамент у
> лучших и оптимизировали инструменты под себя

Самая крупная из набора: новый раздел с нуля, а не правка.

Ограничение, названное сразу (по знаниям на 10.09.2026, проверить при
выполнении — тема живая): LifeHearth это веб-приложение, установленное на
телефон, а не нативное. Доступа к Apple Health у него нет — HealthKit для веба
закрыт, обойти нечем, кроме нативной обёртки. Значит «подтягивать из других
приложений» это один из трёх путей: разбор выгруженного из Здоровья файла;
сервисы с открытым интерфейсом через интернет (Strava, Google Fit и подобные);
ручной ввод, сделанный в два касания.

Часть про здоровье Влад отдал мне: нужны предложения, с разведкой по лучшим
приложениям в этих областях — фундамент от них, оптимизация под себя.

**12.09.2026 — разведка (проверено поиском):**

- Apple Health для веб-приложений закрыт полностью: HealthKit доступен только
  нативным приложениям, обхода нет. Значит «подтянуть из Здоровья» без
  нативной обёртки невозможно, и обещать это нельзя.
- Strava: с 2026 доступ к API только при платной подписке Strava, и новое
  приложение может подключить одного спортсмена — семье не подойдёт.
- Поэтому «из других приложений» = импорт файла: activities.csv из bulk
  export Strava, выгрузка Garmin, своя таблица. Разбор терпимый — даты в
  четырёх форматах, длительность секундами/минутами/ч:мм:сс, вид по словам.

**Что взято у лучших:** недельная цель по ДНЯМ с тренировкой, а не по
записям (Apple Fitness); серия считается по неделям, пропущенный день её не
рвёт (Gentler Streak); «как прошло» четырьмя словами вместо цифр RPE
(Strong/Hevy); точка в календаре цветом вида (Strava).

**Собрано (черновик, канва «Здоровье и спорт», `design/health/`):** раздел
«Здоровье» с вкладками «Спорт» и «Замеры». Спорт: карточка «что сегодня по
ритму через день» с кольцом цели и «последняя — позавчера, бег 5,2 км»;
календарь неделя/месяц с точками; тренировка с плавающей «+» в два касания
(вид + минуты по умолчанию), задним числом — с выбранного дня; сводки: дней на
неделе, за месяц, минуты по видам, средний интервал, серия недель; импорт
CSV с защитой от повторов. Замеры: вес, сон, пульс покоя, давление — тренд за
30 дней, второй замер за день заменяет первый. Таблица `workouts` в синке и
копии; замеры в давно существующих `metrics/metricLogs`.

**Разбор черновика (12.09, 49 агентов: пять взглядов — корректность,
данные/синк, экран, тесты, «как у лучших» — и скептик на каждую находку):**
44 находки, 40 подтверждено, 4 отклонено. Закрыто 33:

- Корректность: месячная сетка дублировала день в сутки перевода часов
  (Берлин, октябрь) — теперь шаг addDays и общий `monthGridKeys` с тестом
  под три часовых пояса; двойной тап по «Сохранить» плодил две записи —
  защита в обеих формах; сон 7,25 округлялся к 7,5, вес уезжал в базу как
  78,30000000000001 — округление по точности замера; два замера на одну
  дату после синка с двух устройств — id лога теперь из метрики и дня,
  тренд берёт правленный позже; при цели 5–7 «сегодня отдых» уводил от
  цели — отдых только при цели ≤4; среднее сна показывало «7 ч 18 м» вместо
  «7 ч 20 м»; дедуп импорта терял вторую пробежку того же дня той же
  длины — ключ с дистанцией; повторный импорт воскрешал удалённое —
  сверка со всей таблицей; импорт — одной транзакцией.
- Данные: SCHEMA_VERSION отстал от Dexie на две версии (копия с
  тренировками прошла бы в старую сборку) — поднят до 23 и сторож-тест;
  вес в профиле и в замерах были двумя числами — связаны в обе стороны;
  удаление замера шло мимо repo — через repo, и в форме появилась кнопка
  удалить замер за день.
- Экран: зоны касания кнопок цели и ячеек календаря (36–43px) — HIT_SLOP;
  подпись дня в выбранной ячейке 3,65:1 — белая; будущие дни не
  выбираются (форма на них не сохранялась без объяснения); плитки — «тренировок
  за месяц», «часов за месяц», «дней между тренировками», «недель подряд
  с целью», без переносов; календарь помнит выбранную неделю после «Замеры
  → Спорт»; ходьба и плавание были одной точкой, бокс тонул в светлой теме;
  «Неделя | Месяц» узкий справа, а не как вторые вкладки; виды в форме
  переносятся, а не прячутся за краем (5 из 9 были не видны); заголовок без
  тире, уезжавшего в начало строки; кегль по шкале проекта.
- Тесты и сторожа: /more/health добавлен в touch, contrast, scale, smoke,
  icons, loading; первый кадр не мигает «Первая тренировка» у того, у кого
  их сотня; ритм «вчера → отдых, позавчера → тренировка» и «когда последний
  раз» проверены в браузере; границы недели — составом, а не числом; месяц
  — точка и стрелки.
- Из «как у лучших»: отметка с «Сегодня» одним жестом (строка появляется
  после первой тренировки; артборд на канве); темп мин/км у бега и ходьбы;
  из анализов — гемоглобин и ферритин с разницей к прошлому (анемия в
  анамнезе; норм приложение не знает и не притворяется).

**Отложено (7):** энергия ↔ тренировки (что даёт силы после ночной смены —
данные оба есть, связки нет; M); сравнение недели с прошлой; готовность к
тренировке по сну и пульсу (Whoop/Gentler — отдельная модель, честно
считать нечем); сон при ночных сменах одной цифрой на календарный день;
форма стартует с «Бег», а не с последнего вида; напоминания об анализах
(есть задачи с повтором — можно ими); Apple Health / Strava — не
возможны, см. выше.

Проверено: 25 юнитов раздела + 3 сетки + 1 схема, 9 браузерных тестов
раздела, полный прогон.

## 15. Раздел «Задачи» — довести целиком · **сделано и проверено**

> Ты как мой гендир улучши раздел Задачи, теперь его время пришло, по всем
> направлениям: дизайн, оптимизация, локализация, структура, и т.п.. Доведи все
> до готового результата и чтобы все было полностью завершено. Можешь еще и сам
> внести свои идеи и разработки, желания и видения дополнить мое видение. Также
> не забудь за сам дизайн внутри приложения и иконок и вообще всего, чтобы
> соответствовал примеальности, топ уровню, красоте и т.п.. Всегда все что
> делаешь с дизайном делай через /design .

ВБИРАЕТ В СЕБЯ шесть пунктов, которые про этот же раздел: 1 (порядок
напоминаний), 2 (чёрная полоса), 4 (направляющие справа), 5 (прикрепление
проекта к проекту), 7 (кнопка «+» у заголовка), 9 (цвет счётчика). Делать их
поштучно, а потом отдельно «доводить раздел», значит трогать одни файлы дважды
и дважды всё перепроверять.

Раздел уже разбирался 08.09.2026 оркестрацией на 14 агентов (7 взглядов +
7 скептиков). Часть находок закрыта, часть отклонена с обоснованием — начинать
с того, что осталось незакрытым, а не с нуля.

Требование «дизайн всегда через /design» вынесено в правила проекта
(CLAUDE.md) — оно постоянное, а не разовое.

**12–13.09 — «разбор остального».** Тридцать находок разбора 08.09
проверены по текущему коду оркестрацией (49 агентов): 16 уже были закрыты в
тот же день, 12 живы, 2 отклонены (браузерные confirm/date — принято как
есть; цвет пустого чекбокса — намеренно). Из живых закрыто десять
(релиз 1.25.0):

- Отпустить задачу на свёрнутую папку теперь можно и чуть выше/ниже неё —
  промах в зазор между папками отдаёт ближайшую (для проектов это было
  починено 08.09, для задач — нет; проверено мутацией).
- Перенос: пересчёт цели раз в кадр вместо двух; порядок задач и проектов
  пишется одной транзакцией (убитая между записями вкладка оставляла
  половину списка со старым порядком).
- Мемоизация ожила: девять useMemo пересчитывались на каждый рендер, потому
  что живой список строился заново; строки задач в memo.
- Подсказка «Жесты списка» говорит, как вложить папку (вправо).
- Строки задач открываются с клавиатуры и озвучиваются; папки — с
  aria-expanded.
- Задача из быстрого ввода докручивается на экран.
- Английский: даты в порядке «месяц день», предпросмотр проекта не режет
  название, одна система кавычек со сторожем.

**Отложено (3):** пустая «Без проекта» как цель переноса — пробовал, секция
внизу меняет геометрию авто-скролла и ловит промахи (два теста переноса
покраснели), вынести задачу из папки пока через форму; три чтения таблицы задач на одном экране и фото в строках
(L, требует переезда фото — частично уже в taskPhotos/taskFiles); две
одинаковые машины переноса задач и проектов (M, рефакторинг без видимого
следа).

Проверено: 4 новых браузерных теста + 31 старый по переносу; полный прогон.

**13.09, свежий взгляд разбора (пять находок, проверил сам по коду и
пробой, релиз 1.26.0):** форма задачи собирала папки на два уровня — папка
третьего уровня в списке отсутствовала, задача из неё показывала «Без
проекта» (мой же недосмотр после трёхуровневой вложенности); на чистом
приложении не было кнопки «Новый проект» — Влад спрашивал; колокольчик в
строке требовал время, хотя напоминания без времени работают с 07.09;
снятая по ошибке отметка с повторяющейся задачи оставляла дубль
следующего повтора (юнит); заполненная форма закрывалась свайпом молча.

## 16. Заглавная буква после автонумерации · **сделано и проверено**

> когда я хочу перейти на следующую нумерацию, я нажимаю на интер, и нумерация
> проставляется автоматически, но когда я начинаю писать текст первое слово не
> начинается с большой буквы, хотя должно начинаться с большой автоматически

Пункт 13.7, вынесен отдельной задачей: к обучению не относится, бьёт каждый день.

Сделано коммитом `7c8481e`. Механизм был только в заметках и опирался на правило
«до каретки в строке пусто» — в задаче перед кареткой стоит «2. », и правило
молчало. Модуль переехал в `lib/`, правило научилось видеть маркер списка,
для textarea добавлен свой обработчик. Проверено мутацией: отключение хука
роняет все три браузерных теста, снятие маркера из правила — ровно те, что про
нумерацию.

## 17. Экран дёргается при долгом наборе · сделано, не проверено

> вот щас пока все это писал заметил что у меня дергается экран приложения во
> время письма продолжительного. Нужно тоже по фиксить

Пункт 13.8, вынесен отдельной задачей по той же причине.

Синтетический набор дрожания не даёт ни в Chromium, ни в WebKit — замерено
покадрово, ноль рывков в обоих. Значит дёргает не набор, а то, что рядом.
Нашлось: на скриншоте владельца в тот момент шёл «Фокус» (25:39), а провайдер
таймера обёрнут вокруг ВСЕГО приложения и пересобирал значение дважды в
секунду. Вместе с ним перерисовывался каждый подписчик — включая форму задачи
(`TaskEditSheet.tsx:138`), которой от таймера нужен один вызов «начать фокус по
задаче», и кнопку «+».

Тикающее время вынесено в отдельный контекст. Сторож
`src/features/focus/pomodoroSubscribers.test.ts` держит правило: зовёшь
`usePomodoro` — обязан показывать время, иначе бери `usePomodoroActions`.
Проверен мутацией. Заведены первые e2e-тесты «Фокуса» (их не было ни одного,
при том что его контекст обёрнут вокруг всего приложения).

ЧЕСТНАЯ ГРАНИЦА: воспроизвести дрожание на устройстве владельца не удалось —
нет ни его клавиатуры, ни объёма данных. Убрана доказанная причина лишних
перерисовок, совпадающая с обстоятельствами жалобы. Подтвердить может только
он: включить «Фокус» и написать длинный текст.

## 18. Пустое поле даты выглядело заполненным · **сделано и проверено**

> Проект начал создаваться, но почему то постоянно весит этот срок и не
> убирается

Пришло как «срочно, не создаются проекты», при разборе оказалось другим.
Создание проектов исправно — это подтвердили два новых сквозных теста
(`e2e/project-create.spec.ts`), которых до сих пор не существовало вовсе.

Настоящая поломка: в новой задаче поле «Срок» показывало сегодняшнее число, и
владелец решил, что срок стоит. Замер: значение поля пустое и до, и после
нажатия «Убрать», ни один чип не подсвечен. Так WebKit рисует НЕЗАПОЛНЕННОЕ
поле даты — подставляет текущую дату вместо подсказки формата. Убирать было
нечего, и правка обработчика «Убрать» не помогла бы.

Починено в общем компоненте поля (`src/components/ui/Input.tsx` + `index.css`),
поэтому чинится во всех семи местах, где приложение спрашивает дату. Коммит
`4cdada2`, проверен мутацией: снятие гашения цвета роняет один тест, снятие
слова «Не задан» — оба.

Побочная находка, НЕ починена здесь: на пустом экране «Задачи» кнопки «Новый
проект» нет вовсе — она появляется, только когда на экране уже что-то есть.
Человеку с чистым приложением создать проект нечем. Починена отдельно —
задача 24.

## 19. Два теста падают только в общем прогоне · **сделано и проверено**

Найдено 10.09.2026 при выкатке задачи 18. В полном прогоне падают
`e2e/ai.spec.ts` («живой провайдер: SSE-стрим…») и `e2e/contrast.spec.ts`
(«dark: контраст текста и иконок не ниже AA»); поодиночке оба зелёные. Значит
дело в соседстве при двух параллельных воркерах, а не в самих проверках.

Неустойчивый тест хуже отсутствующего: он приучает не верить красному. Разобрать
отдельно — с повторными прогонами и, вероятно, с изоляцией общего состояния.

12.09.2026, два полных прогона (299 тестов, ~9 мин каждый): `ai.spec` и
`contrast.spec` из описания не падали. Падали настоящие: «Выбрать» в шапке
заметок 76×26 (зона касания), тест гейта пола ждал старый текст слайда, и
контраст «Включить» в новом баннере «Фокуса» (4.32 при норме 4.5). Все три
починены. Единственные «мигающие» за день — `en-review.spec` и `tasks-dnd.spec`
по одному разу, оба прошли со второй попытки (нагрузка от параллельных
агентов).

Закрыто 20.09 наблюдением, окно вышло восемь дней назад. Оба исходных
подозреваемых — `ai.spec` и `contrast.spec` — с 12.09 не падали ни разу ни в
CI, ни локально. Гейт за то же время дважды поймал настоящие поломки (17.09 —
«Китай» в тесте заморозки, 20.09 — пропущенный focus-timer, см. задачу 24), то
есть красный теперь значит «сломано», а не «соседи мешают». Полный локальный
прогон 20.09: 326 прошли, один мигнул (`icons.spec`, прошёл ретраем) —
единичные мигания ловятся штатным ретраем, изоляция общего состояния не
понадобилась.

**Найдено по пути и починено.** Три деплоя подряд с 11.09 09:17 UTC были
красные из-за тех же двух тестов — релизы 1.20 и 1.21 сутки не были на проде,
хотя пульт писал «сделано и проверено». Это моя ошибка учёта: статус ставился
после локального прогона, а не после зелёного деплоя. С 12.09 правило — статус
только после `success` в Actions. Второе следствие: уведомление «вышло
обновление» о 1.20–1.22 не уходило — CI сравнивал версию с предыдущим
коммитом, а после красного деплоя следующий видел «не менялась». Теперь версию
помнит воркер: рассылка одна на версию, сколько бы раз её ни звали
(`worker/notifyUpdate.test.ts`).

## 20. Прогон на движке Safari · **сделано и проверено**

Заведён отдельный конфиг `playwright.webkit.config.ts`: тот же набор тестов на
движке, где живут владелец и семья. Повод — WebKit в этом приложении уже дважды
повёл себя иначе, чем Chromium: пустое поле даты рисовал сегодняшним числом
(задача 18) и не так применял правило к псевдоэлементу поля.

Первый же прогон окупился: тест поля даты в WebKit упал — там форма открывается
медленнее, и проверка приходила раньше неё. Поломки в коде не было, но тест был
слепым к более медленному движку. Теперь оба движка зелёные.

---

## 21. Обучение под срок: сроки, часы, план курса · **сделано и проверено**

Задача не из набора Влада — пришла отдельной просьбой 10.09: «запихни трекер
обучения в приложение и адаптируй для любого обучения».

Раздел «Обучение» уже был: прогресс в страницах, уроках и процентах, степпер,
история отметок. Не хватало времени: сколько осталось, успеваю ли к сроку,
сколько нагонять. Материал можно было вести месяцами и не заметить, что не
успеваешь, — ровно это и случилось с МВА самого Влада: семь месяцев материалы
качались, ни одна дисциплина не закрыта.

Добавлено: срок у материала (необязательный), запись занятий в часах, расчёт
графика, свой экран материала с планом по частям и журналом, ввод плана
текстом целиком, четвёртая единица прогресса «часы».

**Решение по данным.** План лежит отдельной таблицей `learningParts` (v21), а
не массивом внутри материала. Синхронизация сравнивает `updatedAt` целой
строки и кладёт её через `put` целиком (`sync.ts:124`, `:319`) — массив из
шестидесяти частей терял бы чужие галочки пачкой, отдельные строки расходятся
по одной. Прецедент в ту же сторону: `Task.photos` выселили из строки в v20.
Поля `dueDate`, `minutes`, `note` опциональные, поэтому версии не потребовали.

Дизайн прошёл через `/design`: артборды в `design/learning/`, канва —
https://claude.ai/code/artifact/02862f65-9740-4940-b322-6deb0f57f76f

**Проверено живьём** (dev-сервер, мобильный вьюпорт 375×812): карточка со
сроком и отставанием, экран материала, отметка части плана пересчитывает
прогресс и остаток, запись занятия создаёт лог и двигает темп. Расчёт сошёлся
с ручным: 30 дней из 206 дают план 51 ч при нулевом прогрессе — столько и
показано; 350 ч за 25 недель — 13,9 ч в неделю.

По ходу проверки вылезли две вещи и обе починены (977b6dc): логика записи
занятия жила внутри компонента и кликами не проверялась — вынесена в
`session.ts` и закрыта восемью тестами (главный: два занятия за день
складываются); под графиком стояло «Отставание 51» без единицы.

**Итого:** 604 юнита, сборка, линтер, браузерный тест. **Не проверено:**
`worker/familyRoom` и `family/removal.integration` не идут без Node 22 (в
среде 20.20.2).

## 22. «Фокус» по скринам Focus To-Do · **сделано и проверено**

> Вот, это скрины из приложения помодоро, переними их опыт и добавь нам то
> чего недостает

Скрины 12.09 (Focus To-Do): экран блокировки с живым таймером, «Мелодия
томата» / «Мелодия отдыха» по ~30 мелодий с длительностью, громкость,
уведомление за 5 минут, вибрация, «Белый шум» с 16 звуками и громкостью,
таймер на весь экран со «строгим режимом».

**Сделано (релиз 1.25.0, артборды в `design/focus/`: «Звуки одной
карточкой», «Выбор мелодии», «На весь экран»):**

- Мелодии — 15 вместо 5 (синтез, без файлов): без звука, мягкий,
  колокольчик, двойной, гонг, таймер, звонок, велозвонок, ветряные
  колокольчики, будильник, пианино, кукушка, птицы, фанфары, свисток. Каждая
  с длительностью в списке, тап проигрывает. Тридцать «Музыка1/2/3» не
  делал: их не различить, а значит и не выбрать.
- Конец фокуса и конец перерыва — разные мелодии. Старый единый сигнал
  становится сигналом конца фокуса.
- Громкость мелодии и громкость шума — ползунками, шум меняется на ходу.
- Шумы — 12: дождь, ветер, море, ручей, костёр, сверчки, тиканье, метроном,
  белый/розовый/коричневый. Кофейни и библиотеки нет: голоса и шорох страниц
  шумом не синтезируются, а плохая имитация хуже отсутствия.
- «Предупредить за 5 минут»: пуш через воркер и локальное уведомление, если
  приложение открыто.
- Режим на весь экран: круг, время, круг N из M, три кнопки; экран не гаснет
  (Wake Lock; в Safari работает с 16.4).

**Не взято, честно почему:** живой таймер на экране блокировки — это Live
Activity, веб-приложению закрыто; строгий режим — в вебе не запереть;
длительность вибрации — на iPhone вибрации в браузере нет вовсе.

**Отложено:** счётчик помидоров на задаче («44.8 / 1» на скрине) — нужна
история сессий по задачам, отдельная таблица; секундомер вместо обратного
отсчёта.

Проверено: 14 юнитов (каталоги + докрутка), 11 браузерных тестов «Фокуса»,
сторожа касания/контраста/шкалы/смоук зелёные.

## 8. Сквозной прогон всего приложения · сделано и проверено (на проде 1.27.0)

**13–17.09.** Шестнадцать агентов прошли по разделам в браузере
(iPhone 14 Pro, 393px) — 15 из 16 отчитались (английский режим упёрся в
лимит сессии), 127 находок. Скептиков на каждую находку запустить не
удалось: сотня агентов дважды упиралась в лимит. Проверял сам, по коду и
пробой; 40 закрыты релизом 1.27.0 (перечень — в «Что нового»). Первый деплой
1.27.0 упал из-за теста (`getByText('Китай')` ловил и заголовок проекта, и
строку шита) — починен отдельным коммитом, второй деплой зелёный 17.09.

**Самое весомое из закрытого:** пункт чеклиста без Enter пропадал при
сохранении; настройки «Женских дней» открывались по прямому адресу в обход
кода, и «Убрать код» снимал замок без кода; смена длительности «Фокуса» на
паузе обнуляла идущий круг; удалённый вес оставался в профиле и ИМТ;
копия с пустым списком файлов задач стирала их при восстановлении;
«7,5» в счётчике привычки снимало отметку дня; из копилки снималось
больше накопленного; тап по подписи «Статус» в форме цели молча менял
значение.

**Отложено, по темам (87):**

- *Зоны касания меньше 44px* — **сделано, на проде 1.29.0**: все места
  из прогона закрыты (тумблеры разделов, «+» напоминаний, кольцо привычки,
  степперы обучения, кружки цвета, точки тура, крестики очистки, группы и
  участники семьи, «Обзор за год», корзина, шапки заметки и ИИ-чата,
  карточка и фото места, форма задачи, «Заморожено», строка защиты
  данных на «Сегодня»). Touch e2e расширен, 6/6 зелёных, деплой зелёный.
  Осталось проверить на живом проде.
- *Обучение* — **сделано, 1.36.0** (задача 32). Было (раздел из 1.21): план без оценок обнуляет прогресс; плитка
  «Финиш» делит уроки на часы; у материала в процентах план ломает шкалу;
  «1 уроков» не склоняется; «42 уроков в неделю»; экран удалённого
  материала остаётся открытым; прогресс дважды на карточке. Отдельный
  заход — там нужна модель, а не заплатки.
- *Женские дни* — **сделано, 1.36.0** (задача 32). Было: цикл с пропуском 65 дней молча выпадает из прогноза, но
  раздувает «в среднем»; «меньше обычного» после первой отметки; прогноз
  объясняет разброс не тем; дубль «подождите 2 с»; автозадачи цикла после
  смены пола остаются.
- *Семья без сети* — **сделано, 1.36.0** (задача 32). Было: «Failed to fetch» сырым текстом; звонок без связи 30
  секунд «Вызов…»; зелёная точка «в сети» у себя; шаг «＋» в инструкции.
- *Английский* — коллизии ключей **сделано, 1.36.0**; перевод «Что нового» — вопрос владельцу (PROTOCOL §19). Было: «Что нового» по-русски (список изменений только русский —
  перевод 27 выпусков отдельная работа); две коллизии ключей в настройках.
- *Корзина* — название сделано в 1.29.0; охват привычек и тренировок — вопрос владельцу (PROTOCOL §19). Было: название сжато до 70px, дата в три строки; привычки и
  тренировки в корзину не попадают (мягко удалены, но не восстановить).
- *Мелочи* — **сделано, 1.36.0** (задача 32), кроме «Скрыть» — по протоколу «до следующего запуска», починено «до перехода на вкладку». Было: пустая категория «Прочее — 0 ₽», «Осталось 0 дней», красная
  «Просрочена» у завершённой цели, тап по дню соседнего месяца не листает
  календарь, задача восстановленная после удаления проекта исчезает из
  списка (проект в корзине), запрос поиска стирается после «назад»,
  оранжевая точка «нужна копия» на пустом профиле, «Скрыть» карточки
  данных до перезагрузки, «Что нового → Открыть» срабатывает раз за сеанс.

## 23. Задачи с iPhone не появлялись на маке · **сделано и проверено**

> Почему то у меня не обновляются задачи в приложении на маке, хотя на айфоне
> я создал новые, реши проблему и сделай так, чтобы они максимально быстро
> обновлялись все данные и изменения в приложении на всех устройствах

**18–20.09.** Записи с телефона до сервера доходили (проверено по боевой
базе). Ломался приём: курсор «отдай всё новее последней полученной» считался
по времени правки с часов устройства-автора. Запись, пришедшая на сервер
позже более свежей (создана без сети, в свёрнутом приложении, часы разошлись),
для мака с ушедшим вперёд курсором была невидима навсегда — сервер честно
отвечал «нового нет». В базе 399 из 409 записей лежали не в порядке меток.

**Сделано (1.28.0, коммит `d6be199`):** курсор по порядковому номеру прихода
на сервер; живой сигнал о чужих правках через Durable Object (сокет, как в
семейном чате) — правка появляется на другом устройстве через секунды, опрос
раз в минуту остаётся запасным; отправка перед сворачиванием; после обновления
каждое устройство один раз перечитывает всё — так на мак приедет и то, что
раньше не дошло. «Перечитать всё заново» и восстановление из копии теперь
возвращают и собственные записи устройства.

**Проверено:** 726 юнитов, 327 браузерных, живой `wrangler dev`; два раунда
адверсариального ревью (79 + 50 агентов) — 21 подтверждённая находка закрыта,
среди них: воркер выкатывается из CI раньше ручной миграции (колонки теперь
заводятся сами), новый клиент со старым воркером ломался на preflight,
починка нумерации давала всем строкам один номер. Деплой воркера зелёный,
ленивая миграция на боевой базе прошла (421 запись пронумерована).

**Ограничение, которое не обойти:** iPhone в фоне PWA не выполняет —
обновление там приходит в момент открытия приложения, зато сразу.

## 24. Кнопка «+» в «Задачах» не давала завести проект · **сделано и проверено**

Побочная находка задачи 18 (см. пункт «Пустое поле даты», 10.09): на пустом
экране «Задачи» кнопки «Новый проект» не было вовсе — она появлялась, только
когда на экране уже что-то есть. Даже когда кнопка появлялась, её приходилось
искать отдельно от «+», которая всегда открывала форму задачи напрямую.

Сделано (1.30.0): «+» открывает шторку выбора «Что создать?» с двумя
кнопками — «Новая задача» и «Новый проект». Кнопка «Новый проект» под пустым
списком и внизу заполненного оставлена как есть — это второй, более прямой
путь для тех, кто уже смотрит на список проектов.

Проверено: 53 браузерных теста раздела «Задачи» (перенос, зоны касания,
контраст, i18n, автозаглавные, файлы, период, напоминания, клавиатура) —
все учли новый шаг и прошли; 726 юнитов не задеты.

Дожато 20.09 (1.31.0): в форме новой задачи кнопка «Создать» поднята сразу
после «Заметок» — простую задачу без файлов и чек-листа можно сохранить,
не долистывая форму до низа.

Пропуск, пойманный гейтом деплоя 20.09: среди обновлённых тестов не был учтён
`focus-timer.spec` («при работающем таймере в задаче можно спокойно писать») —
он тоже открывает форму через «+», но живёт в разделе «Фокуса». Упал в CI
дважды, деплой 1.30–1.31 остановлен. Причина найдена локально тем же прогоном:
тест открывал форму старым путём, без шага «Новая задача» в новой шторке.
Поправлен; заодно в нём появилось ожидание видимости поля. Повторная выкатка
20.09 зелёная — на проде 1.31.0.

## 25. Спорт: составные тренировки и шаблоны · сделано, не проверено

Сделано 20.09 в Abacus (1.32.0, `c524b6c`): несколько видов тренировки одной
сессией, сохранение сессии шаблоном и применение в одно касание, своё
название упражнения, недельный и месячный календарь листается пальцем. CI
перед выкладкой зелёный (сборка, линтер, юниты, e2e). Отдельной проверки
раздела не было; артбордов шторки шаблонов в `design/` нет.

Сверка 24.09 нашла в шторках тренировки и шаблонов кнопки 26–40px без
расширения зоны («Убрать», «Добавить ещё вид», «Из шаблона», «Сделать из
этого набора шаблон») и размеры иконок числами. Закрыто в 1.35.0; места
добавлены в `e2e/touch.spec.ts` — аудит по адресам их не видел, шторки
открываются тапом.

## 26. Семья: задачи и чат на весь экран · сделано, не проверено

Сделано 20.09 в Abacus (1.33.0, `862184e`): у семейной задачи свой цвет,
приоритет цветной полосой слева, напоминание о сроке, перетаскивание
удержанием, срок снимается одной кнопкой; чат на весь экран; звонок
сворачивается стрелкой «назад» и идёт в фоне. CI зелёный, отдельной проверки
не было.

Сверка 24.09 нашла и закрыла в 1.35.0: кнопка «Свернуть звонок» с обычным
`HIT_SLOP_44` получала `relative`, а он перебивает `absolute` (проверено в
браузере) — стрелка стояла не в левом углу, а в потоке экрана звонка; плашка
свёрнутого звонка ~34px; кружки цвета 32px при `gap-2` и чипы исполнителя 38px
без расширения зоны. Долги, которые не трогал: свой переключатель приоритета
вместо `SegmentedControl`, перенос удержанием написан заново мимо
`useHoldToReorder`, два oklch-литерала в палитре упражнений.

## 27. «Главная» по пять разделов и экран приоритета · **сделано и проверено**

Сделано 20.09 в Abacus (`81ca91b`): «Главная» показывает первые пять разделов
и кнопку «Показать ещё» (`HOME_VISIBLE_STEP`), в «Настроить разделы» — номер
приоритета у каждого, после обучения — обязательный экран расстановки без
кнопки «Пропустить». Показывается один раз, в том числе тем, кто пользовался
приложением раньше, — то есть всей семье.

Пропуск, найденный сверкой 24.09: записи в «Что нового» не было — новый
обязательный экран появлялся без объяснения. Запись добавлена в 1.35.0.
Тесты в CI сперва падали: экран перехватывал клики во всех e2e (`2df7c07`).
Артборда в `design/` нет.

Ошибка, найденная сверкой 24.09: стрелки «Поднять» и «Опустить» стояли с
зазором 2px при невидимых зонах по 44 — зона нижней лежала поверх почти всей
верхней (замер: 26×44px), и тап по «Поднять» опускал раздел. Стрелки
разведены центрами ровно на 44px (1.35.0), проверка — в `e2e/touch.spec.ts`,
на старом коде падает. Долг: переключатель скрытия — свой, без
`role="switch"`, вместо общего `Switch`.

## 28. Удалить семейную группу · **сделано и проверено**

Начато 20.09 в Abacus и брошено незакоммиченным: код не собирался (две
объявленные и неиспользуемые константы в шторке при `noUnusedLocals`), тестов
не было. Доделано 24.09 (1.35.0):

- создатель удаляет группу из «Участников»; сервер ставит надгробие, рвёт
  живые соединения кодом 4404 («удалена», в отличие от 4403 «исключён»),
  стирает переписку, задачи, участников и конверты ключей;
- дыра сервера, найденная при доделке: тикет, выданный до удаления, всё ещё
  открывал сокет — в пустую комнату снова начали бы писать. Закрыто: удалённая
  комната отвечает 410 и на `/ws`, невыкупленные тикеты и незавершённые
  дозвоны стираются вместе с ней;
- у остальных участников переписка остаётся, над чатом — «Эту группу удалил
  её создатель»;
- шторка: константы оказались брошенной попыткой задачи 29 (выяснилось
  из логов чата Abacus); жест закрытия на шапке получил порог 12px и отмену
  при уходе пальца вбок, а само «плавание» закрыто в задаче 29.

Проверено локально: 6 тестов комнаты (`worker/familyRoom.test.ts`), 2
сквозных клиент-против-сервера (`removal.integration.test.ts`), 3 e2e шторки
(`sheet-drag.spec.ts`), 2 e2e баннера (`family-gone.spec.ts`); тест тикета,
e2e шторки и баннера проверены мутацией — на старом коде падают. Все юниты
на Node 22 (797), сборка и линтер зелёные; полный e2e на итоговом коде —
332 прошли, 0 упали, 2 плавающих прошли на повторе (`ai.spec`,
`recovery-new-phone`, к правке отношения не имеют).

Попутно: `e2e/task-period.spec.ts` («с 10 по 25 сентября») был зелёным ровно
до 23.09 — подпись окна зависит от сегодняшней даты, и с 24.09 тест падал на
любом коде, то есть заблокировал бы следующую выкатку. Часы в нём прибиты к
1 августа.

Ограничение: у участника со старой версией приложения (до 1.35.0) баннера не
будет — его приложение увидит отказ сервера и будет тихо переподключаться раз
в минуту, пока не обновится само при следующем запуске.

**На проде 24.09 (1.35.0).** Деплой `35999144154` зелёный целиком (типы,
линт, юниты, три шарда e2e, сборка, выкладка), воркер — `35999143736`;
прод отдаёт 1.35.0 и новый `push-sw.js`; пуш «Что нового» ушёл на 10
устройств. Задачи 11, 27, 28, 30 — закрыты; 29 и 31 ждут проверки пальцем на
iPhone; 25 и 26 — только правки зон, полного разбора разделов не было.

## 29. Окно создания задачи плавает вбок · сделано, не проверено

> При создании задачи, когда я пальцем опускаюсь в низ я могу двигать
> границы контекстного окна, нужно сделать так, чтобы это окно было мертвым
> и никуда не двигалось в право-лево не плавало

Поставлено 20.09 в чате Abacus; там начато и брошено (две константы в
`Sheet.tsx` без логики — из-за них не собиралась сборка). Найдено 24.09 по
логам чата, которые прислал Влад: на GitHub и пульте этой задачи не было.

Причина по коду: панель шторки листалась в обе стороны (`overflow-y-auto`
даёт и `overflow-x: auto`), и стоило чему-то внутри оказаться шире —
в iOS так делает поле даты со своей минимальной шириной, — вся форма уезжала
пальцем вбок; `overscroll-contain` не пускал прокрутку на страницу под
панелью, но пружину внутри неё оставлял — края окна отъезжали в конце формы.
Сделано (1.35.0): `overflow-x-hidden` и `overscroll-none`.

Проверено: e2e `sheet-drag.spec.ts` держит сам запрет (вычисленные стили
панели при заведомо широком содержимом); на коде из HEAD падает. В Chromium
и настольном WebKit форма в ширину не вылезает вовсе, а сенсорный жест вбок
в эмуляции не синтезировать (колесо в мобильном режиме вбок не листает,
протокол браузера отказывает) — **палец на iPhone проверить Владу.**

## 30. Повтор «каждый месяц» ставил 1-е число · **сделано и проверено**

Правка из чата Abacus 20.09 уехала молча внутри семейного коммита `862184e`
(1.33.0): в «Что нового» её не было, на пульте — тоже. Число повтора в новой
задаче теперь берётся из срока (было 1-е по умолчанию), следующий раз
считается от месяца после срока. Abacus добавил 292 строки юнитов
`recurrence.test.ts`, но напоминание не проверял; на вопрос «добавить тест
напоминания?» Влад ответил «конечно» — тест не был написан.

Сделано 24.09: `taskActions.test.ts` — задача «каждый месяц 10-го» в 9:00 с
напоминанием за 30 минут, отмеченная в срок, на день позже и на три недели
позже: следующий раз — 10 ноября, напоминание — в 8:30 (мутация «число → 1»
роняет все три). Запись в «Что нового» — 1.35.0.

Спорное, принятое в Abacus: если срок стоит раньше дня повтора (срок 5-го,
повтор «10-го»), следующий раз теперь 10-е следующего месяца, а не этого.
Когда число берётся из срока — а это теперь поведение по умолчанию, —
разницы нет. Старые задачи, у которых повтор уже сохранён на 1-е, сами не
исправятся — их число нужно поправить в форме.

## 31. Тап по уведомлению вёл на старый адрес · сделано, не проверено

Найдено 24.09 сверкой протокола. 07.09 приложение переехало с `/life-hub/`
на `/lifehearth/` (`148a29b`), а `public/push-sw.js` остался со старыми
адресами: при закрытом приложении тап по напоминанию или сообщению семьи
открывал `/life-hub/` — там живёт страница-объявление о переезде (отвечает
200); при открытом — мост `SwNavBridge` получал чужой префикс и роутер
показывал «Такого экрана нет». Иконка уведомления тоже тянулась со старого
адреса.

Сделано (1.35.0): база в `push-sw.js` берётся из области действия SW
(`registration.scope`), старый префикс в уже пришедших уведомлениях
переводится на новый и в SW, и в мосте. Проверено: `src/lib/pushSw.test.ts`
гоняет настоящий файл SW в подменённом окружении (5 тестов, на старом файле
падают 4); `e2e/push-open.spec.ts` — мост со старым адресом (без правки
падает). Живой тап на iPhone — вручную: у кого старый SW, заработает после
первого открытия новой версии.

Не тронуто, долг: сторож версии в воркере (`APP_INDEX_URL`,
`worker/src/index.js:844`) по крону сравнивает хэш старой страницы — она
заморожена, и сторож молчит. Рассылку об обновлении делает CI (`notify`),
так что вреда нет; оживлять его — риск двойных пушей всей семье.

## 32. Хвосты сквозного прогона и разбор работы 20.09 · сделано, не проверено

Поставлено Владом 24.09: «продолжай работу над незакрытыми задачами». Взято
всё, что не требует его решения: отложенные ошибки задачи 8 по темам
(мелочи, семья без сети, «Женские дни», модель «Обучения», коллизии
английского) и полный разбор работы Abacus 20.09 в спорте (25) и семейных
задачах (26). Продуктовые идеи из отложенного (история «Фокуса», автостарт
круга, связка энергии и тренировок) без слова Влада не брались.

Как делалось: восемь исполнителей, каждый в своём git worktree и на своём
порту e2e, за каждым — скептик на перепроверку; второй круг закрыл находки
скептиков. Сведено в `main` без конфликтов, 1.36.0 — одна запись в «Что
нового» на всё.

Что нашли скептики и что закрыто вторым кругом (показательно — одна
перепроверка поймала больше, чем первый проход): в «Обучении» отметка части
затирала цель, поправленную руками, а сохранение плана — ручной прогресс; в
«Женских днях» цикл с одним забытым днём выпадал из статистики, карточка
«разной длины» считала по другой выборке, два теста стали холостыми; в
семье перенос пальцем мог «залипнуть» с заблокированной прокруткой и ставил
задачу на строку выше; поиск писал адрес на каждую букву, и Safari после
100 записей за 10 с бросал ошибку; двойное нажатие «Добавить» в спорте
заводило упражнение дважды.

Проверено: юниты 848/848 на Node 22, tsc -b, eslint 0 ошибок, полный e2e
перед пушем — 365 прошли, 0 упали, 1 плавающий (новый
`data-protection-signals` «Скрыть…»: под нагрузкой полного прогона раз упёрся
в таймаут 30 с, отдельно 12/12 — под наблюдением); новые тесты проверены
мутацией.

Первый деплой 1.36.0 (`36047457313`) красный: `appearance.spec` считал слова
«Что нового» на экране и ждал 2, а пункт новой записи со словами «Что нового»
дал 3. Полный e2e был прогнан ДО записи — урок в PROTOCOL §18 п.26. Тест
теперь проверяет заголовок окна. Повторный чистый полный e2e — 365 прошли,
0 упали, 1 плавающий (`task-period` с прибитыми часами — под наблюдением);
под параллельной нагрузкой плавали переносы подпроекта (отдельно 16/16). Не проверено: живой iPhone
(перенос пальцем в семье — только Chromium, CDP-касания в WebKit не идут).

**РЕШАЕТ ВЛАД** (варианты и рекомендация — у каждой):

1. Семья: кому приходит напоминание о семейной задаче. Сейчас — на телефон
   того, кто поставил его последним. (а) оставить и прямо написать в форме;
   (б) исполнителю или всем — нужна правка сервера (подписки уже в комнате).
2. Семья: цвет задачи перекрывает полосу приоритета (белая полоса на белой
   карточке — контраст 1:1). Рекомендую: полоса — всегда приоритет, как в
   личных задачах, цвет — на кружке. Через канву.
3. Семья, приватность: название семейной задачи с напоминанием уходит на
   сервер открытым текстом, а `docs/family-chat-security.md` обещает
   обратное. (а) нейтральный заголовок пуша «Общая задача» + строка в
   документе, что время срабатывания серверу видно; (б) оставить и честно
   переписать документ. Рекомендую (а).
4. Обучение: сохранение плана с оценками ставит цель = сумма оценок при
   любой правке, даже переименовании (так с 10.09). Рекомендую то же
   правило, что уже у прогресса: цель следует за планом, только если была
   выведена из него (~2 строки).
5. «Женские дни»: одиночный цикл вне 19–45 дней без пропусков «не
   учитывается» и не зажигает карточку «разной длины»; а подтвердить
   настоящий длинный цикл (60+) в интерфейсе нечем. Оставить или добавить
   «подтвердить цикл».
6. Пустой профиль: плитка синка на «Главной» и факты «нет копии» в
   настройках копий — оранжевые. Оставить (честные факты) или сделать
   нейтральными.
7. Поиск помнит прошлый запрос и при новом входе до перезапуска. Оставить
   или стирать при входе не через «назад».

Долги, замеченные по пути, не взяты (мелкие, без решения не нужны): ветка
«Выкл» у напоминания семейной задачи без теста; перенос нижней задачи
наверх переписывает всю группу (корень — дробный порядок); личные «Задачи»,
вероятно, с тем же зазором удержания, что был в семье; в «Обучении» три
шаблона без неразрывного пробела; месячная сетка спорта не листается
тапом по серому дню; «Отчёт для врача» считает среднее по всем циклам.

## Не входит в набор

- **Восстановление из файловой копии** стирает записи, появившиеся после снятия
  копии, и показывает опись копии вместо разницы с тем, что сейчас на телефоне.
  Найдено при работе над 1.15.0, Владу озвучено, в десятку не входит.
